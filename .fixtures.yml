---
fixtures:
  repositories:
    stdlib:
      repo: https://github.com/simp/puppetlabs-stdlib
      branch: master
    simplib:
      repo: https://github.com/simp/pupmod-simp-simplib
      branch: master
  symlinks:
    haveged: "#{source_dir}"
